(function (window) {
    window.__env = window.__env || {};

    window.__env.apiUrl = "<joka backend url>/api";
    window.__env.jokaLifeUrl = "<joka-life base url>";
    window.__env.loginPageRedirectUrl = `${window.__env.jokaLifeUrl}?redirectUrl=`;
}(this));